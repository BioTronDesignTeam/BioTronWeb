export interface ModelProps {
  /** Accent colour for emissive highlights (hex string). */
  color: string;
  /** Whether idle animation runs (disabled under reduced motion). */
  animate?: boolean;
}

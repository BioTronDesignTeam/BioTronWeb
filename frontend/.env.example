# Copy to frontend/.env.local (gitignored) to override.
# Base URL of the Go backend (auth + telemetry API).
# Dev default (if unset) is http://localhost:8080; prod is the api. subdomain.
VITE_API_URL="http://localhost:8080"

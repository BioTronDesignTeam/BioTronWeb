# Runs only when apps/exo or something it depends on changes.
name: exo

on:
  pull_request:
    paths:
      - apps/exo/**
      - packages/style/**
      - go/**
      - package-lock.json
      - go.work
  push:
    branches: [main]
    paths:
      - apps/exo/**
      - packages/style/**
      - go/**
      - package-lock.json
      - go.work

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build -w apps/exo/frontend && npm run lint

  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.27'
      - run: go vet ./... && go test ./...
        working-directory: apps/exo/backend

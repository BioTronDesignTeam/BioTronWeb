# Runs only when apps/auth or something it depends on changes.
name: auth

on:
  pull_request:
    paths:
      - apps/auth/**
      - packages/style/**
      - go/**
      - package-lock.json
      - go.work
  push:
    branches: [main]
    paths:
      - apps/auth/**
      - packages/style/**
      - go/**
      - package-lock.json
      - go.work

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build -w apps/auth/frontend

  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.27'
      - run: go vet ./... && go test ./...
        working-directory: apps/auth/backend
